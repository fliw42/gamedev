using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class iche : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("A");
        Debug.Log("pon");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
